scalar JSON

type Color {
  id: ID!
  section1: JSON
}

type Query {
  color: [Color!]!
}

input UpdateColorInput {
  section1: JSON
}

type Mutation {
  updateColor(input: UpdateColorInput!): Color
}
